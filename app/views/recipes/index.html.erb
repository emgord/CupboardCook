<h1>Recipes</h1>

<% @recipes.each do |recipe| %>
  <%= link_to recipe.title, recipe.original_url %>
  <%= image_tag(recipe.image) %>
  yield: <%= recipe.yield %>
  time: <%= recipe.time %>
  description: <%= recipe.description %>
<% end %>
